# Backend Daily Record - 2026-08-26

## Objective

Complete Task 4 review fixes for Redis request limiting, leaderboard cache locking and ranking, workout OpenAPI validation documentation, and validation-envelope coverage.

## Completed

- Replaced the multi-command sorted-set limiter with one asynchronous Redis Lua operation that removes expired requests, checks the limit, records an allowed request, and refreshes the window TTL atomically.
- Updated workout and leaderboard routes to await the async Redis limiter.
- Added a unique lock ownership token and Lua compare-and-delete release so an expired lock's earlier owner cannot remove a later owner's lock.
- Kept weekly leaderboard payload caching at 60 seconds and added a PostgreSQL-backed current-user rank query that uses the same Monday-to-next-Monday boundary and ordering as the top-50 query.
- Declared the workout route's runtime `422` `ErrorEnvelope` in OpenAPI and covered invalid duration, invalid non-cardio distance, and future timestamps through the API envelope.

## Evidence

- Red: `./.venv/bin/pytest tests/unit/test_rate_limit.py tests/integration/test_leaderboard_cache.py tests/api/test_workouts_and_leaderboard.py -q` reported `6 failed, 7 passed`. The failures exposed the synchronous limiter path, unconditional lock delete, missing out-of-top-50 rank source, and default OpenAPI validation schema.
- Green: `./.venv/bin/pytest tests/unit/test_rate_limit.py tests/integration/test_workout_transaction.py tests/integration/test_leaderboard_cache.py tests/api/test_workouts_and_leaderboard.py -q` reported `15 passed`.
- Static checks: `./.venv/bin/ruff check .` reported `All checks passed!`; `./.venv/bin/mypy app` reported `Success: no issues found in 26 source files`.
- Full test suite: `./.venv/bin/pytest -q` reported `28 passed, 4 skipped`.
- The focused limiter test schedules 100 simultaneous requests and permits exactly 10. The cache test holds the first owner past lock expiry, acquires a second owner, then proves the first release leaves the second lock in place.

## Lesson Learned

Redis command sequences that read, decide, and write must be represented as one server-side atomic operation; making a client method asynchronous alone does not establish concurrency safety. Lock expiry also changes ownership, so release must verify the owner token.

## Risk or Blocker

`binding spec`, `task-4-brief.md`, `task-4-report.md`, and `task-4-review.md` were not present anywhere in this workspace, so their requirements could not be independently read. Docker-backed PostgreSQL/Redis integration tests remain skipped on this host, and no Firebase credentials were configured; no Docker or Firebase credentials were added or used.

## Next Action

When Docker and Firebase prerequisites are available, run the async Redis Lua operations against Redis and the current-user-rank query against migrated PostgreSQL with real authenticated requests.
