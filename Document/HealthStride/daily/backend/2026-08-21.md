# Backend Daily Record - 2026-08-21

## Objective

Create the August vertical-slice transactional PostgreSQL schema, reversible Alembic migration, deterministic seed, and point-award rules.

## Completed

- Added async SQLAlchemy models for users, workout logs, points transactions, and the workout catalog.
- Added the reversible baseline Alembic migration and deterministic seed command.
- Added point-rule unit coverage and a PostgreSQL migration integration check.
- Chose provisional deterministic point multipliers because the approved scope specifies caps but not a formula: cardio is 5 points/minute, weight lifting is 4 points/minute, and yoga is 3 points/minute before the 300-point workout and 500-point daily caps.

## Evidence

- The point-rule tests were first run before the domain module existed and failed on the missing `app.features` import.
- The same focused unit tests passed after the domain implementation.
- Container-backed migration evidence is recorded in `evidence/migrations/2026-08-21-up-down.md`; no container commands were run because Docker Desktop is unavailable.

## Lesson Learned

The approved caps provide a stable boundary for a provisional scoring formula, allowing the formula to remain isolated in a pure domain function until product approval supplies final values.

## Risk or Blocker

Docker Desktop is unavailable on this host. PostgreSQL migration, 1,000-row seed, downgrade, and re-upgrade verification require Docker Desktop to be installed and running.

## Next Action

Run the documented Docker Compose, Alembic, seed, test, downgrade, and re-upgrade sequence once the host prerequisite is available.
